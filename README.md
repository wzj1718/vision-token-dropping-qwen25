# vision-token-dropping-qwen25

Codebase for vision-token dropping analysis on Qwen2.5-VL.

## Structure
- `analysis/`: analysis scripts and utilities
- `scripts/`: run scripts (update paths as needed)

## Notes
Large artifacts (checkpoints, npz/npy, logs) are ignored by default via `.gitignore`.
